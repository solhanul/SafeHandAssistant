# The project is intentionally stored in a Korean-named folder.  This lets the
# Android Gradle Plugin build it on Windows while retaining that project name.
android.overridePathCheck=true
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
